apiVersion: v2
name: dastgate
description: >-
  Scheduled DAST (OWASP ZAP by Checkmarx via the Automation Framework) against
  deployed environments, reimported into DefectDojo. Phase 1: a nightly passive
  ZAP baseline CronJob.
type: application

# `version` is the chart version; `appVersion` tracks the dastgate image tag.
version: 0.1.0
appVersion: "0.1.0"

keywords:
  - dast
  - security
  - zap
  - defectdojo
  - cronjob

home: https://github.com/MagmaMoose/dastgate
sources:
  - https://github.com/MagmaMoose/dastgate

maintainers:
  - name: Caleb Sargeant
    email: caleb@magmamoose.com
